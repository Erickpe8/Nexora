{
  "version": 2,
  "installCommand": "npm install --include=dev && npm run vercel-install",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "mobile/dist",
  "cleanUrls": true,
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/:path*", "destination": "/" }
  ]
}
